Streams and Strings
===================

For simpler use, all string formatting utilities are available for use with file output streams
and string streams. Both of these are based on the idea of building up the output string from
parts before either writing it to a file or just using result as a string directly.



.. toctree::
    :caption: Streams
    :maxdepth: 1

    streams/string_stream
    streams/output_stream
